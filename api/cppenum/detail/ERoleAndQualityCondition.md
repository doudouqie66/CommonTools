---
title: ERoleAndQualityCondition
---

# ERoleAndQualityCondition

 

## Variables

| Name | Type | Description |
|------|------|-------------|
| Self_Low |  |   |
| Self_Middle |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Self_High |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Team_Low |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Team_Middle |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Team_High |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Enemy_Low |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Enemy_Middle |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Enemy_High |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Self_UltraHigh |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Team_UltraHigh |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Enemy_UltraHigh |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Self_Higher_High |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Team_Higher_High |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| Enemy_Higher_High |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue_Low |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue_Middle |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue_High |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue_UltraHigh |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue_Simulate_Low |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue_Simulate_Middle |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue_Simulate_High |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue_Simulate_UltraHigh |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue_Self |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue_Team |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue_Enemy |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue_Simulate |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible_SetValue |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| AllVisible |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |