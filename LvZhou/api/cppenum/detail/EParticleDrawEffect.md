---
title: EParticleDrawEffect
---

# EParticleDrawEffect

 

## Variables

| Name | Type | Description |
|------|------|-------------|
| None |  |   |
| NoBlending |  |   |
| PreMultipliedAlpha |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| NoGamma |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| InvertAlpha |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| NoPixelSnapping |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| DisabledEffect |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| IgnoreTextureAlpha |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |
| ReverseGamma |  | -- the value appears as -1 more due to UHT parse error rather than it actually b |